{
  "file": "src/commands/doctor-workspace-status.ts",
  "description": "工作区状态诊断",
  "replacements": {
    "`Fix: ${updateCommands[0]} && ${formatCliCommand(\"openclaw gateway restart\")}.`": "`修复：${updateCommands[0]} && ${formatCliCommand(\"openclaw gateway restart\")}。`",
    "\"Fix each drifted plugin:\"": "\"逐个修复漂移插件：\"",
    "`Then run ${formatCliCommand(\"openclaw gateway restart\")}.`": "`然后运行 ${formatCliCommand(\"openclaw gateway restart\")}。`",
    "note(lines.join(\"\\n\"), \"Plugins\")": "note(lines.join(\"\\n\"), \"插件\")",
    "\"Plugin diagnostics\"": "\"\\n\"",
    "\"npm registry target was not resolved\"": "\"npm registry 目标未解析\"",
    "`No install command generated; retry openclaw doctor after checking registry availability (${targetError}).`": "`未生成安装命令；检查 registry 可用性后重试 openclaw doctor（${targetError}）。`",
    "`Repair target resolution failed for ${entry.pluginId}: ${detail}. No install command generated.`": "`插件 ${entry.pluginId} 的修复目标解析失败：${detail}。未生成安装命令。`",
    "`Active official plugins match post-restart OpenClaw ${drift.gatewayVersion}, but the running Gateway is ${runningGatewayVersion}.`": "`活动官方插件与重启后的 OpenClaw ${drift.gatewayVersion} 匹配，但运行中的网关为 ${runningGatewayVersion}。`",
    "`Plugin ${entry.pluginId} is ${entry.installedVersion}, but a Gateway restart will load OpenClaw ${drift.gatewayVersion}.${runningGatewayVersion ? ` The running Gateway is ${runningGatewayVersion}.` : \"\"}${updateCommand ? \"\" : ` Repair target resolution failed: ${targetError}.`}`": "`插件 ${entry.pluginId} 当前为 ${entry.installedVersion}，但网关重启后会加载 OpenClaw ${drift.gatewayVersion}.${runningGatewayVersion ? ` 当前运行中的网关为 ${runningGatewayVersion}。` : \"\"}${updateCommand ? \"\" : ` 修复目标解析失败：${targetError}。`}`",
    "`Could not check plugin restart readiness: ${readiness.reason}`": "`无法检查插件重启就绪状态：${readiness.reason}`",
    "\"Repair the Gateway service installation, then rerun openclaw doctor before restarting.\"": "\"修复网关服务安装，然后在重启前重新运行 openclaw doctor。\"",
    "\"Update or replace the plugin so it no longer depends on legacy compatibility paths.\"": "\"更新或替换该插件，使其不再依赖旧版兼容路径。\"",
    "\"Plugin restart readiness\"": "\"插件重启就绪状态\"",
    "\"Plugin compatibility\"": "\"插件兼容性\"",
    "`${drift.drifts.length} active official plugin${\n      drift.drifts.length === 1 ? \"\" : \"s\"\n    } not on post-restart OpenClaw ${drift.gatewayVersion}`": "`${drift.drifts.length} 个活动官方插件未处于重启后的 OpenClaw ${drift.gatewayVersion}`",
    "Errors: ": "错误：",
    "Running Gateway: OpenClaw ": "运行中的网关：OpenClaw ",
    " -> expected ": " -> 预期 "
  }
}
